The platform backend never performs infrastructure changes directly.
All execution is delegated to CI/CD pipelines triggered via Git events or APIs.
Pipelines are the single source of execution truth.